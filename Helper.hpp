#ifndef HELPER_H
#define HELPER_H

#include <cmath>

static int manhattanDistance(std::pair<int, int> item1, std::pair<int, int>item2);
int manhattanDistance();
item2 = abs(x2-x1)


//formula abs(int x2 - int x1) + abs(y2 - y1)

#endif