#ifndef GRID_ITEM_H
#define GRID_ITEM_H

#include "Interactable.hpp"


class Goal: public Interactable {
    public:

    int Goal:: goal(int width, int height);
    if interact() || experimentCount() >= 1;
    return false;

 } else {
    return getType()
 }
    


#endif 