#ifndef GRID_ITEM_H
#define GRID_ITEM_H

#include <cmath>
#include <vector>
#include <iostream>



class Game{
    public:

    Game(int width, int height, std::vector<std::pair<int, int>> experimentCoordinates);

bool displayState
std::cout << "You win!" << std::getGameState(W) << std::endl;
return true;
} else {
    std::count << "You lose :(" << std::getGameState(L)<< std::endl;

} 


enum GameState{

WIN = "W";
LOSE = "L";
PLAYING = "P";

}

void movePlayer( int dx, int dy){
    Scientist::movePlayer (dx, dy);
    if gameState == "P"
    bool displayState();
    return GameState;
    
}
#endif 